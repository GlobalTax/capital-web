import React from 'react';
import { MarketReportsManager } from '@/components/admin/MarketReportsManager';

const MarketReports: React.FC = () => {
  return (
    <div className="space-y-6">
      <MarketReportsManager />
    </div>
  );
};

export default MarketReports;