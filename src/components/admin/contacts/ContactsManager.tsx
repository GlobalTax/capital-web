// ============= CONTACTS MANAGER (LEGACY) =============
// Redirects to new contacts-v2 system

import React from 'react';
import { ContactsLayout } from '../contacts-v2';

const ContactsManager = () => {
  return <ContactsLayout />;
};

export default ContactsManager;
