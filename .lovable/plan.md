

## Plan: Eliminar "(últimos 100)" del título del Pipeline

### Cambio

**Archivo**: `src/features/leads-pipeline/components/LeadsPipelineView.tsx` (línea 371)

Cambiar el texto `${totalLeads} leads (últimos 100)` por `${totalLeads} leads`.

